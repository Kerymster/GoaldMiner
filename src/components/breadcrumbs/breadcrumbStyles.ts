/**
 * Breadcrumb trail — list, separators, link + current page styles.
 * Keep in sync with `src/components/Breadcrumbs.tsx`.
 */

export const breadcrumbListClass =
  'flex flex-wrap items-center gap-x-1 gap-y-1 text-sm text-text-muted dark:text-text-subtle'

export const breadcrumbSeparatorClass = 'text-border-default dark:text-border-inverse'

export const breadcrumbLinkClass =
  'font-medium text-text-secondary underline-offset-4 hover:text-gold-700 hover:underline dark:text-fume-300 dark:hover:text-gold-400'

export const breadcrumbCurrentClass = 'font-medium text-text-primary dark:text-text-inverse'
