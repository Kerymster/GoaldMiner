/**
 * Breadcrumb trail — list, separators, link + current page styles.
 * Keep in sync with `src/components/Breadcrumbs.tsx`.
 */

export const breadcrumbListClass =
  'flex flex-wrap items-center gap-x-1 gap-y-1 text-sm text-fume-500 dark:text-fume-400'

export const breadcrumbSeparatorClass = 'text-fume-300 dark:text-fume-600'

export const breadcrumbLinkClass =
  'font-medium text-fume-700 underline-offset-4 hover:text-gold-700 hover:underline dark:text-fume-300 dark:hover:text-gold-400'

export const breadcrumbCurrentClass = 'font-medium text-fume-900 dark:text-fume-100'
